import React from 'react';
import { BrowserRouter, Route, Link } from 'react-router-dom';

const Routes = (props) => (
	<BrowserRouter>
		<div>
			Start!
		</div>
	</BrowserRouter>
);

export default Routes;
